# 版本更新日志

[English](CHANGELOG.md) | 简体中文

这里记录每个正式版本面向用户的功能、行为和安全变化。使用方法请查看
[中文 README](README.zh-CN.md)。

## 0.9.1 - 2026-09-11

### 调整

- Fleet 只读执行除了 `readOnlyPrograms` 外，还必须匹配内置正向 argv profile；
  未知程序和未知参数组合默认拒绝。
- 只读 `git` 缩小到 `status` 和 `rev-parse`；`find`、`sed`、`journalctl`、`ip`
  等复杂程序在新增经过审查和测试的专用 profile 或工具前不支持只读执行；
  `restricted` 模式的模板不能绕过只读策略。
- 单服务器和 Fleet 工具统一增加输出 Schema，并在兼容 JSON 文本之外返回
  `structuredContent`。
- 服务器清单和使用提示改为通过 `get_server_info` 按需读取，不再塞入启动说明。
- MCP 运行时版本统一从 `package.json` 读取。
- 新增 `smoke:readonly`，用于不修改远端文件的真实主机验证。

### 修复

- `--help` 和 `-h` 现在正常显示帮助并以成功状态退出。
- 审计事件现在会把工具组禁用、传输不支持与真正通过策略的操作区分开。

### 安全性

- 只读执行拒绝环境变量覆盖和 stdin，并返回稳定拒绝元数据与解释性的
  `next_step`，不再建议实际不可用的工具路径。
- 文档明确说明程序白名单不是操作系统安全沙箱。

## 0.9.0 - 2026-09-10

### 新增

- 新增健康感知 SSH 连接池 `health-pool-v2`，支持响应新鲜度、轻量空闲
  `ping`、后台补充备用连接以及分类错误信息。
- 身份缓存增加五分钟有效期，并与连接池代数和最近成功响应绑定。
- 只有内置 `probe_identity` 和 `ping` 可以在传输故障后进行一次有限重试。
- 新增解析安全的本地代码检查 CLI，以及项目级 Codex 检查规则。
- 新增固定的 `npm run verify` 发布前验证流水线，子进程全部使用参数数组启动。

### 调整

- 一条经过验证的连接即可服务请求，备用连接改为后台补齐。
- 每一条新连接执行用户操作前都必须验证远端身份。
- 繁忙连接跳过应用心跳；陈旧空闲连接在使用前先执行轻量 `ping`。
- Fleet 中存在进行中请求时，拒绝关闭连接池。

### 安全性

- SSH 结果不确定时，不会自动重放用户命令、文件操作或训练启动。
- 控制器关闭后会取消延迟重试和正在进行的连接池补充，避免连接重新出现。
- 复杂正则从 JSON 文件读取，不再经过 PowerShell 多层引号解析。

### 文档

- 新增中文版 README 和本中文版更新日志。
- README 顶部增加零基础推荐阅读顺序，并明确 README 与 Changelog 的职责。
